{
  "scripts": {
    "test": "cypress open",
    "test:headless": "cypress run --browser chrome && npm run open:report",
    "open:report": "allure serve allure-results"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "allure-commandline": "^2.36.0",
    "allure-cypress": "^3.4.3",
    "cypress": "^14.5.4"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/tests/steps/**/*.js",
      "cypress/tests/steps/**/*.ts"
    ]
  }
}
